package algorithm;

/**
 * @author ltw
 * on 2019-12-22.
 */
public class Xier {
    public void xierSort(int[] tmp) {
        int size = tmp.length;
        while (size / 2 > 0) {
            int group = size / 2;
            for (int i = 0; i < group; i++) {

            }
        }
    }

    public static void main(String[] agrs) {
        Xier xier = new Xier();
        int[] a = {3, 5, 3, 6, 2, 5, 7};
        xier.xierSort(a);
    }
}
